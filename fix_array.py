import json

with open('generate_tutorials.py', 'r', encoding='utf-8') as f:
    lines = f.readlines()

# Instead of parsing python, I can just modify the text
# Or simply use python to parse the file, move the block, and save.
